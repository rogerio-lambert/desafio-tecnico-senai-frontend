import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7c7c50',
    },
    secondary: {
      main: '#ebebe0',
    },
    tertiary: {
      main: '#e67700',
    },
  },
});

export default theme